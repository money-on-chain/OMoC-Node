# `$ disk_clean.sh`

A script tool to clean an Ubuntu Linux instance intended to run via Docker an oMoC Node.

## Example of use

Must be run as root

```shell
root@server:~# curl https://raw.githubusercontent.com/money-on-chain/OMoC-Node/master/Docker/scripts/disk_clean.sh | bash
```
